import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import MajesticPredictor from "./MajesticPredictor";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <MajesticPredictor />
  </React.StrictMode>
);
